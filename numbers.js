// Using the appropriate assignment operator, reassign the value of num1 to the current value plus 17.
let  num1 = 32;
console.log(num1 += 17)

// Using the appropriate assignment operator, reassign the value of num2 to the current value minus 14.
let num2 = 25;
console.log(num2 -= 14);

// Using the appropriate assignment operator, reassign the value of num3 to the current value times 11.
let num3 = 92;
console.log(num3 *= 11);

// Using the appropriate assignment operator, reassign the value of num4 to the current value divided by 8.
let num4 = 64;
console.log(num4 /= 8);